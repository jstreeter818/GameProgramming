class AddBlockComponent extends Component{
    snakeBlocks = []
    constructor(){
        super()
    }
    update(){
        // this will be the same as the eat component
        // should they be combined?
    }
}
window.AddBlockComponent = AddBlockComponent
export default AddBlockComponent